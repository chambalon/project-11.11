import tkinter as tk
import os
import shutil


#class FileManagerApp:


if __name__ == '__main__':
  # Main window
  root = tk.Tk()
  label = tk.Label(root, text="File Manager")
  label.pack()
  # Keep the window open by running the Tkinter event loop
  #root.mainloop()
